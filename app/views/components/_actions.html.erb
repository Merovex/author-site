<% if can? :edit, obj %>
    <%= link_to 'Edit', edit_polymorphic_path(obj), class: 'mx-3 font-sans text-blue-500 dark:text-blue-200 visited:text-violet-500 dark:visited:text-violet-400' %>
<% end %>
